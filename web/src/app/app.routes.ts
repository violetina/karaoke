import { Routes } from '@angular/router';

import { DashboardShellComponent } from './dashboard/dashboard-shell.component';

export const routes: Routes = [{ path: '', component: DashboardShellComponent }];

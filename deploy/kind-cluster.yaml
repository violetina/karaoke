kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: karaoke
nodes:
  - role: control-plane
    extraPortMappings:
      # OpenSearch NodePort -> localhost:9200 on the host
      - containerPort: 30920
        hostPort: 9200
        protocol: TCP

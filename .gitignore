.venv/
__pycache__/
*.pyc
*.egg-info/
.env
.pytest_cache/
build/
dist/
venv/
site/
docs_confluence/
*.py[cod]
.mypy_cache/
.ruff_cache/
.cache/
.tools/
.env.*
!.env.example
.DS_Store
*.webm
.venv
.venv-audio/

# Hermes agent scratch (plans, local notes)
.hermes/

# Local scratch / IDE files
.spyproject/
.claude/
optimize_karaoke_credit_usage
scripts/tst-gemi.py.py

# Vector backup exports: data, not source. Regenerate with
# `python scripts/vector_backup.py export`. Copy these off the machine --
# Postgres has no backup of its own here, so they are the only copy that
# survives losing the disk.
backups/
